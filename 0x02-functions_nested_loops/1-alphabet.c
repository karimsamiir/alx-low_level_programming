#include"main.h"

/**
 * print_alphapet - A function that prints the alphabet, in lowercase, followed by a new line.
*/

void print_alphapet(void)
{
	int ch;

	for (ch = 'a' ch <= 'z'; ch++)
		_putchar(ch);
	-putchar('\n');
}
